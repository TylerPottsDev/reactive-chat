import React from 'react';

const Header = props => (
  <header>
    <section className="title-bar">
      <h1>Reactive<span>Chat</span></h1>
      <h3>React x Firebase</h3>
    </section>
  </header>
)

export default Header;
